# Shared sync-protocol test vectors, vendored from the public main repo.
#
# Source: ShivaClaw/careerseeker  docs/sync-vectors/v1/
# Pinned commit: 679a3175590dcd021b21c85af9daf12114e131fd
# Generator:     docs/sync-vectors/generate.mjs (Node, deterministic)
#
# These are consumed unmodified by ProtocolVectorsTest. They must stay byte-identical to
# the main repo -- that identity is what makes cross-repo protocol drift a CI failure
# rather than a field bug. CI re-fetches this commit's vectors and diffs (see ci.yml).
# To update: bump the pin, re-vendor, and expect the C# SyncHarness to move in lockstep.
