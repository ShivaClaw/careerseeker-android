<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Compose supplies the real theming; this only needs to give the Activity a sane
      window before Compose takes over. No AppCompat dependency is pulled in for it.
    -->
    <style name="Theme.CareerSeeker" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
